<%= simple_form_for([piece, booking]) do |f|%>

 <div class="separated"><%= f.input :start_on, as: :date, html5: true %>
  <%= f.input :end_on, as: :date, html5: true %>
  </div>

  <div class="form-actions" >
    <%= f.button :submit, class: "btn-primary"%>
  </div>

<% end %>
