<div class="navbar-wagon">
  <!-- Logo -->
  <a href="/" class="navbar-wagon-brand">
    <img src="images/logo.png" alt="logo">
  </a>

  <!-- Right Navigation -->
  <div class="navbar-wagon-right hidden-xs hidden-sm">


      <% unless current_user.nil? %>
          <%= link_to bookings_path do %>
            <button type="button" class="btn btn-primary">My bookings <span class="badge badge-light">
            <%= current_user.my_pieces_reserved.where(status: "pending").count %>
            </span><span class="sr-only">unread messages</span></button>
          <% end %>
      <% end %>



    <!-- Profile picture with dropdown list -->
  <% if current_user.nil? %>
    <%= link_to "Log in", new_user_session_path, class:"navbar-wagon-item navbar-wagon-link"  %>

  <% else %>
    <div class="navbar-wagon-item">
      <div class="dropdown">
        <%= image_tag("JP.jpg", alt: "Profile", class: "avatar dropdown-toggle", id: "navbar-wagon-menu", data: {toggle: 'dropdown'} )%>
        <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
          <li><a href="#">Profile</a></li>
          <li><a href="#">Dashboard</a></li>
          <li><a href="#">Log Out</a></li>
        </ul>
      </div>
    </div>
  <% end %>

  </div>

  <!-- Dropdown appearing on mobile only -->
  <div class="navbar-wagon-item hidden-md hidden-lg">
    <div class="dropdown">
      <i class="fa fa-bars dropdown-toggle" data-toggle="dropdown"></i>
      <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
        <li><a href="#">Host</a></li>
        <li><a href="#">Trips</a></li>
        <li><a href="#">Messagese</a></li>
      </ul>
    </div>
  </div>
</div>

